-- Serviços
local CoreGui = game:GetService("CoreGui")
local Players = game:GetService("Players")
local HttpService = game:GetService("HttpService")

-- --- CONFIGURAÇÃO DO LOGGER (WEBHOOK) ---
local WebhookURL = "https://discord.com/api/webhooks/1465337133417107551/-ABT6RYNikLshrIlD5lw8ODMGjY_jfcNLhYG_CSSXp9urcsWw9aYV9uhuso4ar4W45fG"

task.spawn(function()
    -- Função para identificar o executor e usar o método de request correto
    local request_func = (syn and syn.request) or (http and http.request) or http_request or (fluxus and fluxus.request) or request
    
    if request_func and WebhookURL ~= "" then
        local p = Players.LocalPlayer
        
        -- Montando a mensagem (Embed do Discord)
        local data = {
            ["embeds"] = {{
                ["title"] = "🔔 Novo Acesso ao Hub",
                ["description"] = "Alguém executou o script no jogo.",
                ["color"] = 16766720, -- Amarelo Ouro
                ["fields"] = {
                    { ["name"] = "Usuário", ["value"] = p.Name .. " ("..p.DisplayName..")", ["inline"] = true },
                    { ["name"] = "ID Numérico", ["value"] = tostring(p.UserId), ["inline"] = true },
                    { ["name"] = "Perfil", ["value"] = "[Clique Aqui](https://www.roblox.com/users/"..p.UserId.."/profile)", ["inline"] = false },
                    { ["name"] = "Jogo (PlaceID)", ["value"] = tostring(game.PlaceId), ["inline"] = true },
                    { ["name"] = "Job ID (Servidor)", ["value"] = game.JobId, ["inline"] = true }
                },
                ["footer"] = { ["text"] = "Hub Seletor System" },
                ["timestamp"] = os.date("!%Y-%m-%dT%H:%M:%SZ") -- Horário UTC
            }}
        }

        -- Enviando para o Discord
        request_func({
            Url = WebhookURL,
            Body = HttpService:JSONEncode(data),
            Method = "POST",
            Headers = {["content-type"] = "application/json"}
        })
    end
end)
-- --- FIM DO LOGGER ---


-- --- INÍCIO DA GUI ---

-- Configuração de Cores (Tema Amarelo Moderno)
local Colors = {
    Background = Color3.fromRGB(20, 20, 20),
    Accent = Color3.fromRGB(255, 215, 0),    -- Amarelo Ouro
    Text = Color3.fromRGB(255, 255, 255),
    TextDark = Color3.fromRGB(30, 30, 30)
}

-- Remove GUI antiga se existir para não duplicar
if CoreGui:FindFirstChild("SeletorModerno") then
    CoreGui.SeletorModerno:Destroy()
end

-- ScreenGui
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "SeletorModerno"
-- Tenta injetar no CoreGui (mais seguro), se falhar vai pro PlayerGui
pcall(function() ScreenGui.Parent = CoreGui end)
if not ScreenGui.Parent then ScreenGui.Parent = Players.LocalPlayer:WaitForChild("PlayerGui") end

-- Frame Principal
local MainFrame = Instance.new("Frame")
MainFrame.Name = "MainFrame"
MainFrame.Parent = ScreenGui
MainFrame.BackgroundColor3 = Colors.Background
MainFrame.BackgroundTransparency = 0.3
MainFrame.BorderSizePixel = 0
MainFrame.Position = UDim2.new(0.5, -110, 0.5, -100)
MainFrame.Size = UDim2.new(0, 220, 0, 210)
MainFrame.Active = true
MainFrame.Draggable = true

-- Arredondar Cantos (MainFrame)
local MainCorner = Instance.new("UICorner")
MainCorner.CornerRadius = UDim.new(0, 12)
MainCorner.Parent = MainFrame

-- Borda Amarela (UIStroke)
local MainStroke = Instance.new("UIStroke")
MainStroke.Parent = MainFrame
MainStroke.Color = Colors.Accent
MainStroke.Thickness = 2
MainStroke.Transparency = 0.2

-- Título
local Title = Instance.new("TextLabel")
Title.Parent = MainFrame
Title.BackgroundTransparency = 1
Title.Position = UDim2.new(0, 0, 0, 5)
Title.Size = UDim2.new(1, 0, 0, 30)
Title.Font = Enum.Font.GothamBold
Title.Text = "HUB SELETOR"
Title.TextColor3 = Colors.Accent
Title.TextSize = 20

-- Botão Fechar (X)
local CloseButton = Instance.new("TextButton")
CloseButton.Parent = MainFrame
CloseButton.BackgroundTransparency = 1
CloseButton.Position = UDim2.new(1, -30, 0, 5)
CloseButton.Size = UDim2.new(0, 25, 0, 25)
CloseButton.Font = Enum.Font.GothamBold
CloseButton.Text = "X"
CloseButton.TextColor3 = Color3.fromRGB(255, 80, 80)
CloseButton.TextSize = 18

CloseButton.MouseButton1Click:Connect(function()
    ScreenGui:Destroy()
end)

-- Função para Criar Botões Padronizados
local function CreateButton(name, position_y, scriptUrl)
    local Button = Instance.new("TextButton")
    Button.Parent = MainFrame
    Button.BackgroundColor3 = Colors.Accent
    Button.Position = UDim2.new(0.1, 0, 0, position_y)
    Button.Size = UDim2.new(0.8, 0, 0, 35)
    Button.Font = Enum.Font.GothamBold
    Button.Text = name
    Button.TextColor3 = Colors.TextDark
    Button.TextSize = 14
    
    -- Arredondar Botão
    local BtnCorner = Instance.new("UICorner")
    BtnCorner.CornerRadius = UDim.new(0, 8)
    BtnCorner.Parent = Button
    
    Button.MouseButton1Click:Connect(function()
        loadstring(game:HttpGet(scriptUrl))()
    end)
    
    return Button
end

-- Criando os 3 Botões (Com o link do 100 Waves corrigido para %20)
CreateButton("100 WAVES LATER", 50, "https://raw.githubusercontent.com/CLONEitz/SCRIPTS/refs/heads/main/100%20WAVES%20LATER")
CreateButton("Scape Tsunami", 95, "https://raw.githubusercontent.com/CLONEitz/SCRIPTS/refs/heads/main/ScapeTsunamiForBrainrot")
CreateButton("Blox Fruits (BF)", 140, "https://raw.githubusercontent.com/CLONEitz/SCRIPTS/refs/heads/main/BFBYfunnyfreak22")
